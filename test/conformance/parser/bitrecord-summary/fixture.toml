id = "parser.bitrecord-summary"
lane = "parser"
status = "implementation-gated"
spec = "kyokaispec/src/language/03-grammar.md; kyokaispec/src/language/05-declarations.md"
edition = "2026"
inputs = ["src/Demo/Bits.kyo"]
command = "kyokai internal conformance-fixture parser.bitrecord-summary"
expect = "Parse the closed bitrecord backing type and item grammar into structured field and reserved-range facts."
expected_outcome = "accept"
expected_stage = "parser"
expected_code = "bitrecord-summary"
expected_facts = ["bitrecord=TcpFlags", "backing=Nat16", "field=fin:bit:0", "field=window:bits:7..4", "reserved=15..8"]
targets = "host-independent"
prooftrace = "FRONTEND-KYOKAI-SURFACE-PARSER"
