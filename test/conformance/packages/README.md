# Packages Conformance Lane

This lane contains workspace resolution, lockfiles, vendoring, package-index metadata, package docs, semver reports, yanks, and offline behavior.

Current status: `workspace-lockfile-graph` executes the Phase 4 precursor slice for workspace-only graph construction and deterministic lockfile rendering through the internal runner. Git/index solving, graph-changing lockfile modes, publication, and public conformance remain open.
